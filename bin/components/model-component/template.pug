script.
  window.modelViewer = window.modelViewer || {};
  window.modelViewer['#{id}'] = !{json}

model-viewer(id=id, title=title model=json)

script.
  document.getElementById('#{id}').model = !{json}