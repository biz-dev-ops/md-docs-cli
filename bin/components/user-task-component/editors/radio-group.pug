label(for=item.id class={required: item.required})=item.label
div(class='input-group')   
  if(!item.required)
    div(class='form-check')
      input(type='radio' name=item.id id=item.id value='')
      label(for=item.id)='none'

  each option in item.editor.options
    div(class='form-check')
      input(type='radio' name=item.id id=option.id value=option.value)
      label(for=option.id)=option.name