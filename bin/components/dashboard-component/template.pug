ul
  each item, key in summary
    li(class='#{name} #{segment.status.type}')
      button(class='item' aria-expanded='true')= segment.total + ' ' + name
      ul
        each value, status in segment
          li(class='#{status}')
            span(class='item')
              span(class='keyword')= status 
              span= value