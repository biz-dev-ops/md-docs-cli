user-task:
  name: Approve information
  schema:
    $ref: "../../data-dictionary/user-tasks/approve_information.yml"
  ui:
    hidden:
      - task_id
actions:
  approve:
    name: Approve
    schema: 
      $ref: "../../data-dictionary/commands/approve_information.yml"
    ui:
      hidden:
        - command_id
        - correlation_id
        - causation_id
  reject:
    name: Reject
    schema: 
      $ref: "../../data-dictionary/commands/reject_information.yml"
    ui:
      hidden:
        - command_id
        - correlation_id
        - causation_id