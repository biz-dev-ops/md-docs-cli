user-task:
  name: Asses purchase order
  schema:
    $ref: "../data-dictionary/user-tasks/assess_purchase_order.yml"
  ui:
    hidden:
      - task_id
      - process_instance_id
actions:
  end:
    name: End
    schema: 
      $ref: "../data-dictionary/commands/end_purchase_order.yml"
    ui:
      hidden:
        - command_id
        - correlation_id
        - causation_id
  extend:
    name: Extend
    schema: 
      $ref: "../data-dictionary/commands/extend_purchase_order.yml"
    ui:
      hidden:
        - command_id
        - correlation_id
        - causation_id
  renew:
    name: Renew
    schema: 
      $ref: "../data-dictionary/commands/renew_purchase_order.yml"
    ui:
      hidden:
        - command_id
        - correlation_id
        - causation_id