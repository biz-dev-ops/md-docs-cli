allOf:
  - $ref: "../../../../data-dictionary/commands/command.yml"
  - type: object
    title: contract supplier
    properties:
      client:
        type: string
        description: |
          @Arjan wat is het data type.
      supplier:
        type: string
        format: email
        description: The suppliers email addres.
      assignment:
        $ref: "../models/assignment.yml"
      information_definitions:
        type: array
        title: information definitions
        minItems: 1
        items:
          $ref: "../types/information_definition_id.yml"
    required:
      - client
      - supplier
      - assignment
      - information_definitions
  