allOf:
  - $ref: "../../../../data-dictionary/commands/command.yml"
  - type: object
    title: reject purchase order
    properties:
      rejection:
        type: string
        format: markdown
    required:
     - rejection